# OGZ MusicStation

A Discord music-bot built with discord.py, yt-dlp, aiohttp, and dotenv.

## Usage

1. `git clone … && cd OGZ-MusicStation`  
2. Create a `.env` at the project root:
   ```text
   DISCORD_TOKEN=your-bot-token-here
